import React from "react";
import { LoginReferenceContainer, ReferenceTitle } from "./layout.styled";

export const LoginReferenceLayout = () => (
  <LoginReferenceContainer>
    <ReferenceTitle>
      Already have an account? <span>Log in</span>.
    </ReferenceTitle>
  </LoginReferenceContainer>
);
