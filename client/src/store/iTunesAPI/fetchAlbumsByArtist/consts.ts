import { createActionType } from "../../types/actions";

export const FETCH_ALBUMS_BY_ARTIST = createActionType(
  "FETCH_ALBUMS_BY_ARTIST"
);

export const CLEAR_ALBUMS_BY_ARTIST_STATE = "CLEAR_ALBUMS_BY_ARTIST_STATE";
