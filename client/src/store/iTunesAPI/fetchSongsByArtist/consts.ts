import { createActionType } from "../../types/actions";

export const FETCH_SONGS_BY_ARTIST = createActionType("FETCH_SONGS_BY_ARTIST");

export const CLEAR_SONGS_BY_ARTIST_STATE = "CLEAR_SONGS_BY_ARTIST_STATE";
